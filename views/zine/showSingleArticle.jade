extends ../layout

block content
  .mainSpace
    //- h1= article.title
    
    a(href='/zine/' + article._id)= article.title
      img.articleImage(src=article.backgroundURL)
    p= article.body
    
    br
    a(href="/zine/" + article._id + "/editArticle") Edit the article!
